const styles = {
  root: {
    height: "100%",
    flexDirection: "column",
    justifyContent: "space-between",
  },
  params: { margin: 2, height: 365 },
  button: {
    justifyContent: "flex-end",
  },
};

export default styles;
